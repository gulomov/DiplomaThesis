plugins {
    id("com.android.library")
    id("org.jetbrains.kotlin.android")
}

android {
    namespace = "com.deploma.work.features"

    defaultConfig {
        minSdk = 24
        compileSdk = 33
    }
}

dependencies {
}
